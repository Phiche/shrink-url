apiVersion: v1
kind: Service
metadata:
  name: {{ include "shrink-chart.fullname" . }}
  labels:
    {{- include "shrink-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shrink-chart.selectorLabels" . | nindent 4 }}
